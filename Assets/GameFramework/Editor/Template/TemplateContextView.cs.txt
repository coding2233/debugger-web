using strange.extensions.context.api;
using strange.extensions.context.impl;
using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using Unity.VisualScripting;
using UnityEngine;

namespace $TemplateNameSpace
{
    public class $TemplateNameContextView : FrameworkContextView
    {
        private void Start()
        {
            context = new $TemplateNameContext(this, ContextStartupFlags.MANUAL_LAUNCH);
            context.Launch();
        }
    }
}